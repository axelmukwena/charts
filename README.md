# Charts Prototyping

### Reproduce

1. Clone the repository
2. Ensure you have `npm` installed
   ```shell
   $ npm -v
   ```
3. Go to repository directory

   ```shell
   $ cd home/charts
   ```

4. Install packages

   ```shell
   $ npm install
   ```

5. Launch

   ```shell
   $ npm start
   ```
